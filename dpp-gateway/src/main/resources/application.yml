server:
  port: 9005

spring:
  application:
    name: dpp-gateway

  cloud:
    nacos:
      discovery:
        server-addr: localhost
        port: 8848
#    gateway:
#      routes:
#        - id: dpp-auth
#          uri: lb://dpp-auth
#          predicates:
#            - Path=/auth/**
#          filters:
#            - StripPrefix=1