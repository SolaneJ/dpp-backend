server:
  port: 8080

spring:
  application:
    name: dpp-gateway

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

  config:
    import:
      - 'classpath:application-route.yml'